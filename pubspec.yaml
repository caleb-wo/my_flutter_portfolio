name: my_portfolio_app
description: "An app made while doing the Udemy course for Flutter found here: https://www.udemy.com/course/flutter-the-guide-to-build-android-ios-and-web-apps/"
publish_to: 'none'
version: 0.1.0+1

environment:
  sdk: ^3.12.0

dependencies:
  flutter:
    sdk: flutter
  flutter_svg: ^2.3.0
  google_fonts: ^8.1.0
  url_launcher: ^6.0.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0

  launcher_name: ^1.0.2

launcher_name:
  default: Caleb Wolfe


flutter:
  uses-material-design: true
  assets:
    - assets/
